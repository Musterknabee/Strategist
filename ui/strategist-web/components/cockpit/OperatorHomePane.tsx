"use client";
import { Pane } from "@/components/terminal/Pane";
import { StatusBadge } from "@/components/operator/StatusBadge";
import type { OperatorModeId } from "@/lib/operator/operator-modes";
import type { OperatorHomeSummary } from "@/lib/operator/operator-home-summary-model";
type Props = { summary: OperatorHomeSummary; onShowAdvanced: () => void; onOpenDetail: (mode: OperatorModeId) => void };
const v = (n: number | null) => (n == null ? "UNKNOWN" : String(n));
export function OperatorHomePane({ summary, onShowAdvanced, onOpenDetail }: Props) {
  return <section className="cockpit-home" data-testid="cockpit-operator-home"><Pane title="Cockpit Home" dense badge={<StatusBadge raw={summary.overall_status} />}><p className="cockpit-home__intro">Paper only - no live trading. This home view is a summary; use details before any operator decision.</p><div className="cockpit-home__cards"><article className="cockpit-home__card"><span>Strategies</span><strong>{v(summary.strategy_count)}</strong></article><article className="cockpit-home__card"><span>Paper Wins</span><strong>{summary.paper_win_count}</strong></article><article className="cockpit-home__card"><span>Paper Losses</span><strong>{summary.paper_loss_count}</strong></article><article className="cockpit-home__card"><span>Blocked</span><strong>{summary.blocked_count}</strong></article><article className="cockpit-home__card"><span>Needs Review</span><strong>{summary.warning_count}</strong></article><article className="cockpit-home__card"><span>Data / Provider Health</span><strong>OK:{summary.provider_ok_count} | Pending key:{summary.provider_pending_key_count}</strong></article></div><div className="cockpit-home__split"><div><h3>What needs attention?</h3><ul data-testid="cockpit-home-attention">{summary.top_attention_items.length > 0 ? summary.top_attention_items.map((item) => <li key={item}>{item}</li>) : <li>No urgent blockers from current read-plane payloads.</li>}</ul></div><div><h3>Next safe action</h3><p data-testid="cockpit-home-next-action"><StatusBadge raw={summary.next_action} /> {summary.next_action_reason}</p><p className="cockpit-home__meta">Generated: {summary.generated_at_utc}</p></div></div><div className="cockpit-home__table-wrap"><table className="term-dense-table" data-testid="cockpit-home-scoreboard"><thead><tr><th>Strategy</th><th>Paper result</th><th>Evidence</th><th>Data health</th><th>Status</th><th>Next action</th><th>Details</th></tr></thead><tbody>{summary.strategy_rows.map((row) => <tr key={row.strategy_id}><td>{row.label}</td><td>{row.paper_result_label}</td><td>{row.evidence_status}</td><td>{row.provider_status}</td><td><StatusBadge raw={row.status} /></td><td>{row.next_action}</td><td><button type="button" className="linkish" onClick={() => onOpenDetail((row.detail_target as OperatorModeId) || "DAILY_OPS")}>Open details</button></td></tr>)}</tbody></table></div><div className="cockpit-home__actions" data-testid="cockpit-home-links"><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("RESEARCH_REVIEW")}>Strategy lifecycle</button><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("FORENSIC_AUDIT")}>Research forensics</button><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("RELEASE_CONTROL")}>Evidence packet</button><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("FORENSIC_AUDIT")}>Audit timeline</button><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("CAPITAL_FIREWALL")}>Capital firewall</button><button type="button" className="term-btn term-btn--sm" onClick={() => onOpenDetail("FIRST_RUN")}>Provider setup</button><button type="button" className="term-btn term-btn--sm" data-testid="cockpit-show-advanced" onClick={onShowAdvanced}>Show advanced cockpit details</button></div></Pane></section>;
}
